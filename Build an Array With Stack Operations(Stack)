class Solution {
    public List<String> buildArray(int[] target, int n) {
        List<String> list=new ArrayList<>();
        int cur=1;

        for(int arr:target){
            while(cur<arr){
                list.add("Push");
                list.add("Pop");
                cur++;
            }
            list.add("Push");
            cur++;
        }
        return list;
    }
}
